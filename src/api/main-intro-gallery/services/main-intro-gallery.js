'use strict';

/**
 * main-intro-gallery service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::main-intro-gallery.main-intro-gallery');
